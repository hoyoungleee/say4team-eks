spring:
  application:
    name: gateway-service
  cloud:
    config:
#      uri: http://config-service:8888 # 도커 서비스 용
#      profile: default
      uri: http://localhost:8888  # 로컬 용
      profile: dev # 공통 application.yml 읽어오겠다
